class Program {
	int arr[1000006];

	int main() {
		int n,i;

		callout ("scanf", n);

		// Scanning the elements
		for i = 0 , n {
			callout ("scanf", arr[i]);
		}

		// Printing the only the even elements present in the array
		for i = 0 , n {
			if ( tmp%2 == 0) {
				callout ("printf",tmp);
				callout ("printf"," ");
			}
		}
		callout ("printf","\n");

		return 0;
	}
}